<h1>Artist Form:</h1>

<%= form_for @artist do |f| %>
    <%= f.label "Artist's Name:" %>
    <%= f.text_field :name %>
    <%= f.label "Artist's Bio:" %>
    <%= f.text_field :bio %>
    <%= f.submit %>
<% end %>